# 说明：
# 本文件展示项目需要的环境变量配置
# 复制为 .env 并修改后使用

# MySQL 配置（用于创建 MySQL 容器）
MYSQL_ROOT_PASSWORD=bluebell321
MYSQL_DATABASE=bluebell

# Redis 配置
# Redis 默认无密码，生产环境可设置

# 后端配置
BLUEBELL_MODE=release

# ====================================
# 重要说明
# ====================================
# 1. 如果修改 MySQL 密码，必须同步修改 settings/config.docker.yaml
#    中的 mysql.password 字段
#
# 2. 生产环境建议：
#    - 修改默认密码
#    - 设置 Redis 密码
#    - 使用强密码（16位+，包含大小写、数字、特殊字符）
#
# 3. 当前配置适用于：
#    - 本地开发测试
#    - 快速部署体验
# ====================================